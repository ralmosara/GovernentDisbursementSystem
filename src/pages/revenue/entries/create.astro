---
import MainLayout from '../../../layouts/MainLayout.astro';
import RevenueEntryForm from '../../../components/forms/revenue/RevenueEntryForm.vue';
import { revenueService } from '../../../lib/services/revenue.service';
import { db } from '../../../lib/db/connection';
import { fundClusters } from '../../../lib/db/schema';

const user = Astro.locals.user;

if (!user) {
  return Astro.redirect('/login');
}

// Fetch data for form dropdowns
const revenueSources = await revenueService.getRevenueSources(true);
const fundClustersData = await db.select().from(fundClusters);
---

<MainLayout title="Record Revenue Entry">
  <div class="container mx-auto px-4 py-6">
    <!-- Breadcrumb -->
    <nav class="flex mb-6" aria-label="Breadcrumb">
      <ol class="inline-flex items-center space-x-1 md:space-x-3">
        <li class="inline-flex items-center">
          <a href="/revenue" class="text-gray-700 hover:text-primary-600">
            Revenue
          </a>
        </li>
        <li>
          <div class="flex items-center">
            <i class="pi pi-angle-right text-gray-400"></i>
            <a href="/revenue/entries" class="ml-1 text-gray-700 hover:text-primary-600 md:ml-2">
              Revenue Entries
            </a>
          </div>
        </li>
        <li aria-current="page">
          <div class="flex items-center">
            <i class="pi pi-angle-right text-gray-400"></i>
            <span class="ml-1 text-gray-500 md:ml-2">Create</span>
          </div>
        </li>
      </ol>
    </nav>

    <!-- Page Header -->
    <div class="mb-6">
      <h1 class="text-3xl font-bold text-gray-900">Record Revenue Entry</h1>
      <p class="mt-1 text-sm text-gray-600">
        Record a new revenue transaction
      </p>
    </div>

    <!-- Form Card -->
    <div class="bg-white rounded-lg shadow border border-gray-200 p-6">
      <RevenueEntryForm
        client:only="vue"
        revenueSourcesData={JSON.stringify(revenueSources)}
        fundClustersData={JSON.stringify(fundClustersData)}
      />
    </div>
  </div>
</MainLayout>
